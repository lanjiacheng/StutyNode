在一个主机中，可以有多个ip（同一时刻只能用一个ip），可以有多个端口，
无论ip怎样换，端口只有那些个。
并且用localhost做主机名，那么绑定的ip是127.0.0.1（此ip只能在本地连接使用），
如果使用的是0.0.0.0那么绑定的会是本机上所有可用ip（即可以远程连接）


子网ip:		即可用的分配ip范围（如是192.168.217.0，可分配的范围是192.168.217.0到192.168.217.127）
gateway:	网关
netMask：	子网掩码
DNS：		域名解析服务器地址
