一、比较运算符：
==
!=
<
>
<=
>=
instanceof（检查是否是类的对象）

几点说明：比较结果都是true或false

二、逻辑运算符：
&	and（与）：两边必定都执行
|	or（或）：两边必定都执行
^	xor（异或）：两边结果相同就是false，两边结果不同就是true
!	not（非）
&&	and（短路）：若左边为false则不再执行右边
||	or（短路）：若左边为true则不再执行右边

几点说明：短路运算符先执行并判断左边，若判断左边就能得出结果，不再执行右边