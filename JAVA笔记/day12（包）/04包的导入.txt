一、包的导入的格式：
import 包名.类名;

注意，import导入的是包中的类，不包括包中的包，如果包中还有包，那么如下导入：
import 包名.包名.类名;

二、导包的原则：用到哪个类，就导哪个类

三、导包的作用：
也就是说import的作用：
就一个作用，简化类名的书写，导入哪个类，书写的时候形式上相当于这个类在当下的
包里面，就可以不用加上包名，但是本质上这个类还是在原来那个包中，它的访问权限不变