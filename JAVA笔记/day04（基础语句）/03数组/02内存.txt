一、内存的划分：
1.寄存器
2.本地方法区（由系统而定）
3.方法区
4.栈内存
	存储的都是变量
	而且变量所属作用域一旦结束，该变量在内存中被释放
5.堆内存
	存储的是数组和对象（其实数组就是对象）凡是new建立的都在堆中
	堆内存当中的数据都有默认值（int:0 double:0.0 boolean:false char:'\u0000'）
	堆中每个实体都有首地址
	垃圾回收机制（没有变量指向的实体，视为垃圾，回收机制不定时回收）