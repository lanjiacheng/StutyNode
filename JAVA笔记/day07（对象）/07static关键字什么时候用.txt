静态什么时候用：
一、静态变量：
1.当对象中所具备的成员变量值都是相同的，可以被修饰成静态的
2.只要数据在对象中都是不同的，就是对象的特有数据，必须存储在对象中，是非静态的
3.如果是相同的数据，对象不需要修改，只需要使用即可，不需要存储在对象中，定义成静态变量

二、静态函数：
1.函数是否用静态修饰，就参考一点，该功能是否访问到对象中的特有数据，不需要就静态，需要就非静态
2.没访问特有数据的方法，该对象的创建是没有意义的
3.非静态需要被对象调用