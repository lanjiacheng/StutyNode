异常处理的原则：
1.函数内容如果抛出需要被监测的异常，那么函数上必须要声明
	否则必须在函数内用try-catch语句捕捉，否则编译失败

2.如果调用到了声明异常的函数，要么try-catch要么throws，否则编译失败

3.什么时候用catch，什么时候用throws呢？
	功能可以解决，用catch
	解决不了，用throws告诉调用者，由调用者解决

4.一个功能如果抛出了多个异常，那么调用时，就必须要有多个对应的catch进行针对性的处理
	内部有几个需要被检测的异常，就抛出几个异常，抛出几个，就catch几个