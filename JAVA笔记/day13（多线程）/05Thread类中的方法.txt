一、getName方法
该方法是非静态的，依赖对象存在，调用该方法返回的是该线程对象的名字（如果没有指定，默认是Thread-编号）

二、构造方法：
1.super():
创建新线程，线程名字是默认值：Thread-编号

2.super(String name):
创建指定名字的新线程

三、currentThread()方法：
该方法是静态的，返回的是当前线程的对象的引用，当前线程指的是调用这个方法的代码所属的线程

